dependencies: selenium, chromedriver
steps:
Place the chromedrive.exe in a C:\drivers\
go to the root dir and open cmd
type python tenderdown.py
It will download the tenders for 800 pages and there are 10 tenders per page, so 8000 tenders